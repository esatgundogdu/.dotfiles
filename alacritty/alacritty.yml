# Window settings
window:
  padding:
    x: 5
    y: 5
  opacity: 0.9

# Keybindinds
key_bindings:
  - { key: V, mods: Control|Shift, action: Paste }

# Color Scheme
import:
  - ~/.config/alacritty/colorscheme/themes/dracula.yaml

font:
  size: 9
